# Cross-device sync (optional). Leave blank to run local-only.
# Create a free project at https://supabase.com, then copy these from
# Project Settings -> API. The anon key is safe to expose (Row-Level
# Security restricts every row to its signed-in owner).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
